#include "application.h"
int main(int argc, char** argv) {
    //    try{
    //        Application app(argc, argv);
    //        app.Run();
    //    } catch (std::exseption){
    //        e.what();
    //    } catch (...);
    Application app(argc, argv);
    app.Run();
}
